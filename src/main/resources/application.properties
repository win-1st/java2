spring.application.name=example304

# ===== PORT =====
server.port=${PORT:8080}

# ===== DATABASE CONFIGURATION =====
# Sử dụng MYSQL_PUBLIC_URL từ Railway
# Parse từ: mysql://root:password@shuttle.proxy.rlwy.net:35597/railway
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:35597/railway?useSSL=false&allowPublicKeyRetrieval=true&requireSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=yuBZFDxeYBdmsoraGNDdHNVwJmXnoyip

# Hoặc dùng biến môi trường (nếu deploy lên Railway)
# spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true&requireSSL=false&serverTimezone=UTC
# spring.datasource.username=${MYSQLUSER}
# spring.datasource.password=${MYSQLPASSWORD}

# ===== JPA/HIBERNATE =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# ===== JWT CONFIGURATION =====
# JWT_SECRET đã được decode (base64): "tathangExample304SpringBootJWTSeurity2024"
bezkoder.app.jwtSecret=mySuperSecureJwtSecretKeyExample3042025!!
bezkoder.app.jwtExpirationMs=86400000

# ===== FILE UPLOAD =====
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads

# ===== WEBSOCKET =====
spring.websocket.allowed-origins=*

# ===== ADDITIONAL DATABASE SETTINGS =====
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10